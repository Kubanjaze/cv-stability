# cv-stability — Phase 41

Reproducibility analysis of RF cross-validation across seeds and fold counts.
Demonstrates LOO-CV stability vs high variance of K-fold on small datasets.

## Usage

```bash
PYTHONUTF8=1 python main.py --input data/compounds.csv
```
